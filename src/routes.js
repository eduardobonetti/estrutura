import React from 'react';
import ListaPedido from "./pages/lista-pedido";

export default () =>{
//Aqui deve conter o componente de rotas do app (https://reactnavigation.org/), deixei a lista apenas para o app não exibir uma tela branca :)
  return <ListaPedido />
}
